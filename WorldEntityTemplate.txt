#ifndef WORLDENTITY_HPP_INCLUDED
#define WORLDENTITY_HPP_INCLUDED

#include <System/WorldEntity.hpp>

class WorldEntityExtended
{
public:
    WorldEntityExtended();
    WorldEntityExtended(float x, float y, float angle);

    ~WorldEntityExtended();

    void kill();
    void initPhysics(GameWorld* world)=0;
    void hit(WorldEntity* entity, GameWorld* gameWorld) {};
    void update(GameWorld& world)=0;
    void render()=0;

private:


};

#endif // WORLDENTITY_HPP_INCLUDED
